<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/surface_card"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <!-- Main content area with iOS-style padding -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:gravity="center_vertical">

        <!-- Green dot indicator for content changes -->
        <View
            android:id="@+id/content_change_indicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/green_dot_indicator"
            android:visibility="invisible"
            android:contentDescription="Content changed" />

        <!-- Category icon -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="12dp">

            <ImageView
                android:id="@+id/category_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:scaleType="fitCenter"
                android:contentDescription="Category icon" />

            <!-- Green checkmark overlay for completed campaigns -->
            <ImageView
                android:id="@+id/category_checkmark"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="bottom|end"
                android:background="@drawable/green_checkmark_circle"
                android:src="@drawable/checked_small"
                android:scaleType="center"
                android:visibility="gone"
                android:contentDescription="Campaign completed" />

        </FrameLayout>

        <!-- Content area -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Issue title -->
            <TextView
                android:id="@+id/issue_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:textColor="@color/text_primary_modern"
                android:textSize="16sp"
                android:fontFamily="sans-serif-medium"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingExtra="1dp" />

            <!-- Contact avatars and action count row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Container for overlapping avatars -->
                <FrameLayout
                    android:id="@+id/avatars_container"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:layout_marginEnd="8dp">

                    <!-- Avatars will be added programmatically here -->

                </FrameLayout>

                <!-- Action type tag (Email/Call) -->
                <TextView
                    android:id="@+id/action_type_tag"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/action_tag_call_background"
                    android:textColor="@color/badge_call_text"
                    android:textSize="11sp"
                    android:fontFamily="sans-serif-medium"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="3dp"
                    android:visibility="gone"
                    android:contentDescription="Action type" />

                <!-- Actions taken text -->
                <TextView
                    android:id="@+id/actions_taken"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="@color/text_secondary_modern"
                    android:textSize="14sp"
                    android:text="• Loading actions..."
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

        </LinearLayout>

        <!-- Hidden old views for backward compatibility -->
        <TextView
            android:id="@+id/issue_call_count"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/previous_call_stats"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- iOS/WhatsApp style separator -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="16dp"
        android:background="#E0E0E0" />

</LinearLayout>
